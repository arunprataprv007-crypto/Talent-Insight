## Packages
(none needed)

## Notes
- App uses Replit Auth (provided by @/hooks/use-auth and @/lib/auth-utils)
- Dark mode is enforced as the primary aesthetic.
- The shared/routes and shared/schema are fully utilized.
